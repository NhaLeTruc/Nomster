<div class="booyah-box col-xs-10 col-xs-offset-1">
	<h1>Enter your place detail</h1>
	<%= simple_form_for @places do |f| %>
		<%= f.input :name %>
		<%= f.input :address %>
		<%= f.input :description %>
		<hr/>
		<%= f.submit 'Create', :class => 'btn btn-primary' %>
	<% end %>
</div>